const divide = function(a, b) {
    return a / b;
  };
  // Now you can call the divide function like this:
  console.log(divide(2000, 100)); // Output: 20


  const square = (x) => x * x;
// Now you can call the square function like this:
console.log(square(2)); // Output: 4



const add = (a, b) => a + b;
// Now you can call the add function like this:
console.log(add(3, 4)); // Output: 7
